import { and,eq,ilike,lte,ne,or } from "drizzle-orm";import { db,pool } from "../../src/db";import { campaigns,contactLists,contacts,lists,messages } from "../../src/db/schema";import { segmentDefinitions } from "../../src/db/segment-schema";import { workerHeartbeats } from "../../src/db/operations-schema";const intervalMs=Math.max(1000,Number(process.env.CAMPAIGN_WORKER_INTERVAL_MS||"5000"));async function heartbeat(meta:Record<string,unknown>={}){await db.insert(workerHeartbeats).values({workerName:"campaign",metadata:meta}).onConflictDoUpdate({target:workerHeartbeats.workerName,set:{lastSeenAt:new Date(),metadata:meta}})}
async function resolveRecipients(list:typeof lists.$inferSelect){if(!list.isDynamic)return db.select({contactId:contacts.id,email:contacts.email}).from(contactLists).innerJoin(contacts,eq(contactLists.contactId,contacts.id)).where(and(eq(contactLists.listId,list.id),eq(contacts.status,"active")));const [rule]=await db.select().from(segmentDefinitions).where(eq(segmentDefinitions.listId,list.id)).limit(1);if(!rule)return [];let condition;if(rule.field==="email_domain")condition=rule.operator==="equals"?ilike(contacts.normalizedEmail,`%@${rule.value}`):ne(contacts.normalizedEmail,rule.value);else if(rule.field==="validation_status")condition=rule.operator==="equals"?eq(contacts.validationStatus,rule.value as "pending"|"valid"|"invalid"|"unknown"|"error"):ne(contacts.validationStatus,rule.value as "pending"|"valid"|"invalid"|"unknown"|"error");else condition=rule.operator==="equals"?eq(contacts.status,rule.value as "active"|"archived"):ne(contacts.status,rule.value as "active"|"archived");return db.select({contactId:contacts.id,email:contacts.email}).from(contacts).where(and(eq(contacts.status,"active"),condition))}
async function runOnce(){const now=new Date();const due=await db.select().from(campaigns).where(or(eq(campaigns.status,"queued"),and(eq(campaigns.status,"scheduled"),lte(campaigns.scheduledAt,now)))).limit(10);let resolved=0;for(const campaign of due){if(!campaign.listId)continue;const [list]=await db.select().from(lists).where(eq(lists.id,campaign.listId)).limit(1);if(!list)continue;const recipients=await resolveRecipients(list);if(recipients.length)await db.insert(messages).values(recipients.map(r=>({campaignId:campaign.id,contactId:r.contactId,recipientEmail:r.email,status:"queued" as const}))).onConflictDoNothing({target:[messages.campaignId,messages.contactId]});await db.update(campaigns).set({status:"sending",updatedAt:new Date()}).where(eq(campaigns.id,campaign.id));resolved+=recipients.length}await heartbeat({state:"online",campaigns:due.length,resolved})}async function main(){while(true){try{await runOnce()}catch(e){console.error("[campaign-worker]",e);await heartbeat({state:"error"}).catch(()=>{})}await new Promise(r=>setTimeout(r,intervalMs))}}main().catch(console.error);process.on("SIGTERM",async()=>{await pool.end();process.exit(0)});
