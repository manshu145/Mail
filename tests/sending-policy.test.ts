import test from "node:test";import assert from "node:assert/strict";import { effectiveWarmupDailyLimit,shouldPauseForReputation } from "../src/lib/sending-policy";
test("warmup starts at day one and grows without exceeding caps",()=>{const p={enabled:true,startedAt:new Date("2026-09-01T00:00:00Z"),dayOneLimit:100,growthPercent:50,maxDailyLimit:1000};assert.equal(effectiveWarmupDailyLimit(p,5000,new Date("2026-09-01T12:00:00Z")),100);assert.equal(effectiveWarmupDailyLimit(p,5000,new Date("2026-09-02T12:00:00Z")),150);assert.equal(effectiveWarmupDailyLimit(p,200,new Date("2026-09-10T00:00:00Z")),200)});
test("reputation does not pause below sample and does pause over thresholds",()=>{assert.equal(shouldPauseForReputation({sent:20,bounced:10,complaints:0,minSample:100,bounceStop:.05,complaintStop:.003}),false);assert.equal(shouldPauseForReputation({sent:100,bounced:5,complaints:0,minSample:100,bounceStop:.05,complaintStop:.003}),true);assert.equal(shouldPauseForReputation({sent:1000,bounced:0,complaints:3,minSample:100,bounceStop:.05,complaintStop:.003}),true)});
