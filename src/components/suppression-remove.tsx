"use client";import { useRouter } from "next/navigation";export function SuppressionRemove({id,email}:{id:string;email:string}){const router=useRouter();return <button className="btn-danger !min-h-8 !px-2.5 !py-1 text-xs" onClick={async()=>{if(!confirm(`Remove global suppression for ${email}? Only do this when the recipient is eligible to receive mail again.`))return;await fetch(`/api/suppressions/${id}`,{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({confirm:true})});router.refresh()}}>Remove</button>}
