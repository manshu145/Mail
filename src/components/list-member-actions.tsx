"use client";import { useRouter } from "next/navigation";import { useState } from "react";export function AddListMember({listId}:{listId:string}){const router=useRouter(),[email,setEmail]=useState(""),[error,setError]=useState("");async function add(){setError("");const r=await fetch(`/api/lists/${listId}/members`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const d=await r.json().catch(()=>({})) as {error?:string};if(!r.ok)return setError(d.error||"Could not add");setEmail("");router.refresh()}return <div><div className="flex gap-2"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="contact@example.com" className="min-w-0 flex-1 rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2 text-sm dark:border-zinc-800 dark:bg-zinc-900"/><button onClick={add} className="btn-primary">Add member</button></div>{error?<p className="mt-2 text-xs font-bold text-rose-600">{error}</p>:null}</div>}
export function RemoveListMember({listId,contactId}:{listId:string;contactId:string}){const router=useRouter();return <button onClick={async()=>{await fetch(`/api/lists/${listId}/members`,{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({contactId})});router.refresh()}} className="btn-danger !min-h-8 !px-2.5 !py-1 text-xs">Remove</button>}
