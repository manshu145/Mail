"use client";import { useRouter } from "next/navigation";import { useState } from "react";export function CampaignControl({id,status}:{id:string;status:string}){const router=useRouter(),[busy,setBusy]=useState(false);if(["completed","cancelled"].includes(status))return null;return <button disabled={busy} className="btn-danger" onClick={async()=>{if(!confirm("Cancel this campaign? Queued, ready and deferred recipient messages will be cancelled. Messages already accepted by the MTA cannot be recalled."))return;setBusy(true);await fetch(`/api/campaigns/${id}/control`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"cancel"})});setBusy(false);router.refresh()}}>{busy?"Cancelling…":"Cancel campaign"}</button>}
