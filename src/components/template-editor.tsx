"use client";import { useRouter } from "next/navigation";import { useState } from "react";export function TemplateEditor({template}:{template:{id:string;name:string;subject:string|null;htmlBody:string;textBody:string}}){const router=useRouter(),[html,setHtml]=useState(template.htmlBody),[busy,setBusy]=useState(false),[msg,setMsg]=useState("");async function save(fd:FormData){setBusy(true);setMsg("");const r=await fetch(`/api/templates/${template.id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({name:fd.get("name"),subject:fd.get("subject"),htmlBody:fd.get("htmlBody"),textBody:fd.get("textBody")})});const d=await r.json().catch(()=>({})) as {error?:string};setBusy(false);if(!r.ok)return setMsg(d.error||"Could not save");setMsg("Saved");router.refresh()}return <div className="grid gap-5 xl:grid-cols-2"><form action={save} className="premium-panel p-5"><div className="grid gap-4"><label className="text-sm font-bold">Name<input name="name" defaultValue={template.name} required className="mt-1.5 w-full rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2.5 dark:border-zinc-800 dark:bg-zinc-900"/></label><label className="text-sm font-bold">Default subject<input name="subject" defaultValue={template.subject||""} className="mt-1.5 w-full rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2.5 dark:border-zinc-800 dark:bg-zinc-900"/></label><label className="text-sm font-bold">HTML body<textarea name="htmlBody" value={html} onChange={e=>setHtml(e.target.value)} rows={14} className="mt-1.5 w-full rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2.5 font-mono text-xs dark:border-zinc-800 dark:bg-zinc-900"/></label><label className="text-sm font-bold">Plain text<textarea name="textBody" defaultValue={template.textBody} rows={7} className="mt-1.5 w-full rounded-xl border border-zinc-200 bg-zinc-50 px-3 py-2.5 text-sm dark:border-zinc-800 dark:bg-zinc-900"/></label></div><div className="mt-4 flex items-center gap-3"><button disabled={busy} className="btn-primary">{busy?"Saving…":"Save template"}</button><span className="text-xs font-bold text-emerald-600">{msg}</span></div></form><section className="premium-panel overflow-hidden"><div className="border-b border-zinc-100 p-4 dark:border-zinc-900"><h2 className="font-black">Sandboxed preview</h2><p className="mt-1 text-xs text-zinc-500">Scripts are disabled in this preview.</p></div><iframe title="Template preview" sandbox="" srcDoc={html} className="h-[720px] w-full bg-white"/></section></div>}
