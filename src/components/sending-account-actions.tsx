"use client";import { useRouter } from "next/navigation";import { useState } from "react";export function SendingAccountActions({id,status,hourly,daily}:{id:string;status:string;hourly:number;daily:number}){const router=useRouter(),[busy,setBusy]=useState(false),[h,setH]=useState(hourly),[d,setD]=useState(daily);async function patch(body:Record<string,unknown>){setBusy(true);await fetch(`/api/sending-accounts/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setBusy(false);router.refresh()}return <div className="mt-4 flex flex-wrap items-end gap-2"><label className="text-xs font-bold text-zinc-500">Hourly<input type="number" min="1" value={h} onChange={e=>setH(Number(e.target.value))} className="mt-1 block w-24 rounded-lg border border-zinc-200 bg-white px-2 py-1.5 dark:border-zinc-800 dark:bg-zinc-900"/></label><label className="text-xs font-bold text-zinc-500">Daily<input type="number" min="1" value={d} onChange={e=>setD(Number(e.target.value))} className="mt-1 block w-28 rounded-lg border border-zinc-200 bg-white px-2 py-1.5 dark:border-zinc-800 dark:bg-zinc-900"/></label><button disabled={busy} onClick={()=>patch({hourlyLimit:h,dailyLimit:d})} className="btn-secondary !min-h-8 !px-2.5 !py-1 text-xs">Save limits</button><button disabled={busy} onClick={()=>patch({status:status==="active"?"paused":"active"})} className="btn-secondary !min-h-8 !px-2.5 !py-1 text-xs">{status==="active"?"Pause":"Activate"}</button></div>}
