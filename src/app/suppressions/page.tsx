import { ShieldBan } from "lucide-react";import { redirect } from "next/navigation";import { desc } from "drizzle-orm";import { AppShell } from "@/components/app-shell";import { ResourceCreate } from "@/components/resource-create";import { SuppressionRemove } from "@/components/suppression-remove";import { db,databaseConfigured } from "@/db";import { suppressions } from "@/db/schema";import { getSession } from "@/lib/auth";export default async function SuppressionsPage(){const session=await getSession();if(!session)redirect("/login");let rows:typeof suppressions.$inferSelect[]=[];let bad=false;if(databaseConfigured)try{rows=await db.select().from(suppressions).orderBy(desc(suppressions.createdAt)).limit(200)}catch{bad=true}const usable=databaseConfigured&&!bad;return <AppShell session={session}><div className="mb-7 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-xs font-extrabold uppercase tracking-[.18em] text-zinc-400">Compliance</p><h1 className="text-3xl font-black tracking-[-.035em] sm:text-4xl">Suppressions</h1><p className="mt-2 text-sm text-zinc-500">Global do-not-send state. Removal is owner-only and audited.</p></div><ResourceCreate disabled={!usable} endpoint="/api/resources/suppressions" title="Suppress address" buttonLabel="Add suppression" fields={[{name:"email",label:"Email",type:"email",required:true},{name:"reason",label:"Reason",type:"select",options:["manual","unsubscribe","hard_bounce","invalid","complaint","policy"]},{name:"note",label:"Note",type:"textarea"}]}/></div><section className="premium-panel overflow-hidden">{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[820px] text-left text-sm"><thead className="bg-zinc-50 text-[11px] font-extrabold uppercase tracking-[.12em] text-zinc-400 dark:bg-zinc-900/70"><tr><th className="px-5 py-3.5">Address</th><th>Reason</th><th>Source</th><th>Added</th><th>Control</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t border-zinc-100 dark:border-zinc-900"><td className="px-5 py-4 font-bold">{r.email}</td><td className="capitalize">{r.reason.replaceAll("_"," ")}</td><td className="text-xs text-zinc-500">{r.source}</td><td className="text-xs text-zinc-500">{new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(r.createdAt)}</td><td>{session.role==="owner"?<SuppressionRemove id={r.id} email={r.email}/>:<span className="text-xs text-zinc-400">Owner only</span>}</td></tr>)}</tbody></table></div>:<div className="grid min-h-72 place-items-center text-center"><div><ShieldBan className="mx-auto h-8 w-8 text-zinc-400"/><h2 className="mt-4 font-black">No suppressed addresses</h2></div></div>}</section></AppShell>}
