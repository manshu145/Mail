import { NextRequest,NextResponse } from "next/server";import { db,databaseConfigured } from "@/db";import { lists } from "@/db/schema";import { segmentDefinitions } from "@/db/segment-schema";import { getSession } from "@/lib/auth";import { audit } from "@/lib/audit";
const fields=new Set(["email_domain","validation_status","contact_status"]),ops=new Set(["equals","not_equals"]);export async function POST(request:NextRequest){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});if(!databaseConfigured)return NextResponse.json({error:"Database unavailable"},{status:503});const b=await request.json().catch(()=>null) as {name?:string;description?:string;type?:string;field?:string;operator?:string;value?:string}|null;const name=b?.name?.trim()||"",dynamic=b?.type==="dynamic";if(!name)return NextResponse.json({error:"List name is required"},{status:400});if(dynamic&&(!fields.has(b?.field||"")||!ops.has(b?.operator||"")||!b?.value?.trim()))return NextResponse.json({error:"Dynamic segments require field, operator and value."},{status:400});try{const result=await db.transaction(async tx=>{const rows=await tx.insert(lists).values({name,description:b?.description?.trim()||null,isDynamic:dynamic}).onConflictDoNothing({target:lists.name}).returning({id:lists.id});if(!rows.length)throw new Error("duplicate");if(dynamic)await tx.insert(segmentDefinitions).values({listId:rows[0].id,field:b!.field as "email_domain"|"validation_status"|"contact_status",operator:b!.operator as "equals"|"not_equals",value:b!.value!.trim().toLowerCase()});return rows[0]});await audit("list.created",session,"list",result.id,{name,dynamic});return NextResponse.json({ok:true,id:result.id},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error&&e.message==="duplicate"?"List name already exists":"Could not create list"},{status:409})}}
