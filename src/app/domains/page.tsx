import { Globe2, ShieldCheck } from "lucide-react";import { redirect } from "next/navigation";import { desc } from "drizzle-orm";import { AppShell } from "@/components/app-shell";import { ResourceCreate } from "@/components/resource-create";import { db,databaseConfigured } from "@/db";import { sendingDomains } from "@/db/operations-schema";import { getSession } from "@/lib/auth";
export default async function DomainsPage(){const session=await getSession();if(!session)redirect("/login");let rows:typeof sendingDomains.$inferSelect[]=[];let bad=false;if(databaseConfigured)try{rows=await db.select().from(sendingDomains).orderBy(desc(sendingDomains.createdAt))}catch{bad=true}const usable=databaseConfigured&&!bad;return <AppShell session={session}><div className="mb-7 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-xs font-extrabold uppercase tracking-[.18em] text-zinc-400">Deliverability</p><h1 className="text-3xl font-black tracking-[-.035em] sm:text-4xl">Sending domains</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-zinc-500">SPF, DKIM and DMARC are checked from DNS. A domain is only marked ready when all three checks pass.</p></div><ResourceCreate disabled={!usable||session.role!=="owner"} endpoint="/api/resources/domains" title="Add sending domain" buttonLabel="Add domain" fields={[{name:"domain",label:"Domain",required:true,placeholder:"mail.example.com"},{name:"trackingDomain",label:"Tracking domain",placeholder:"click.example.com"}]}/></div><section className="premium-panel overflow-hidden">{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-zinc-50 text-[11px] font-extrabold uppercase tracking-[.12em] text-zinc-400 dark:bg-zinc-900/70"><tr><th className="px-5 py-3.5">Domain</th><th>SPF</th><th>DKIM</th><th>DMARC</th><th>Status</th><th>Last checked</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t border-zinc-100 dark:border-zinc-900"><td className="px-5 py-4 font-black">{r.domain}</td>{[r.spfOk,r.dkimOk,r.dmarcOk].map((ok,i)=><td key={i}><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${ok?"bg-emerald-50 text-emerald-700 dark:bg-emerald-950/30 dark:text-emerald-300":"bg-amber-50 text-amber-700 dark:bg-amber-950/30 dark:text-amber-300"}`}>{ok?"Pass":"Needs work"}</span></td>)}<td className="capitalize font-bold">{r.status}</td><td className="text-xs text-zinc-500">{r.lastCheckedAt?new Intl.DateTimeFormat("en",{dateStyle:"medium",timeStyle:"short"}).format(r.lastCheckedAt):"Pending worker check"}</td></tr>)}</tbody></table></div>:<div className="grid min-h-72 place-items-center text-center"><div><Globe2 className="mx-auto h-8 w-8 text-zinc-400"/><h2 className="mt-4 font-black">No sending domains yet</h2><p className="mt-1 text-sm text-zinc-500">Add a domain; the health worker will verify DNS.</p></div></div>}</section></AppShell>}
