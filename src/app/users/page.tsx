import { UsersRound } from "lucide-react";import { redirect } from "next/navigation";import { desc } from "drizzle-orm";import { AppShell } from "@/components/app-shell";import { ResourceCreate } from "@/components/resource-create";import { UserRowActions } from "@/components/user-row-actions";import { db,databaseConfigured } from "@/db";import { users } from "@/db/schema";import { getSession } from "@/lib/auth";
export default async function UsersPage(){const session=await getSession();if(!session)redirect("/login");if(session.role!=="owner")redirect("/dashboard");let rows:typeof users.$inferSelect[]=[];let bad=false;if(databaseConfigured)try{rows=await db.select().from(users).orderBy(desc(users.createdAt))}catch{bad=true}const usable=databaseConfigured&&!bad;return <AppShell session={session}><div className="mb-7 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-xs font-extrabold uppercase tracking-[.18em] text-zinc-400">Security</p><h1 className="text-3xl font-black tracking-[-.035em] sm:text-4xl">Users</h1><p className="mt-2 text-sm text-zinc-500">Owner controls admin/operator access. Session roles are rechecked against the database.</p></div><ResourceCreate disabled={!usable} endpoint="/api/users" title="Create user" buttonLabel="Add user" fields={[{name:"name",label:"Name",required:true},{name:"email",label:"Email",type:"email",required:true},{name:"password",label:"Temporary password (12+ chars)",type:"password",required:true},{name:"role",label:"Role",type:"select",options:["admin","operator"],required:true}]}/></div><section className="premium-panel overflow-hidden">{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-zinc-50 text-[11px] font-extrabold uppercase tracking-[.12em] text-zinc-400 dark:bg-zinc-900/70"><tr><th className="px-5 py-3.5">User</th><th>Role</th><th>Status</th><th>Created</th><th>Controls</th></tr></thead><tbody>{rows.map(u=><tr key={u.id} className="border-t border-zinc-100 dark:border-zinc-900"><td className="px-5 py-4"><div className="font-black">{u.name}</div><div className="text-xs text-zinc-500">{u.email}</div></td><td className="capitalize font-bold">{u.role}</td><td className="capitalize">{u.status}</td><td className="text-xs text-zinc-500">{new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(u.createdAt)}</td><td><UserRowActions id={u.id} role={u.role} status={u.status}/></td></tr>)}</tbody></table></div>:<div className="grid min-h-64 place-items-center text-center"><div><UsersRound className="mx-auto h-8 w-8 text-zinc-400"/><h2 className="mt-4 font-black">No database users available</h2></div></div>}</section></AppShell>}
