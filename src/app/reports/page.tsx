import { BarChart3,MailCheck,MousePointerClick,ShieldAlert } from "lucide-react";import { redirect } from "next/navigation";import { desc,sql } from "drizzle-orm";import { AppShell } from "@/components/app-shell";import { db,databaseConfigured } from "@/db";import { messageEvents,messages } from "@/db/schema";import { getSession } from "@/lib/auth";
export default async function ReportsPage(){const session=await getSession();if(!session)redirect("/login");let delivered=0,bounced=0,deferred=0,accepted=0,opens=0,clicks=0,complaints=0,unsubs=0,total=0,dbError=false;let recent:typeof messages.$inferSelect[]=[];if(databaseConfigured)try{const [t,d,b,df,a,o,c,cp,u,r]=await Promise.all([db.select({value:sql<number>`count(*)::int`}).from(messages),db.select({value:sql<number>`count(*)::int`}).from(messages).where(sql`${messages.status}='delivered'`),db.select({value:sql<number>`count(*)::int`}).from(messages).where(sql`${messages.status}='bounced'`),db.select({value:sql<number>`count(*)::int`}).from(messages).where(sql`${messages.status}='deferred'`),db.select({value:sql<number>`count(*)::int`}).from(messages).where(sql`${messages.status}='mta_accepted'`),db.select({value:sql<number>`count(*)::int`}).from(messageEvents).where(sql`${messageEvents.type}='open'`),db.select({value:sql<number>`count(*)::int`}).from(messageEvents).where(sql`${messageEvents.type}='click'`),db.select({value:sql<number>`count(*)::int`}).from(messageEvents).where(sql`${messageEvents.type}='complaint'`),db.select({value:sql<number>`count(*)::int`}).from(messageEvents).where(sql`${messageEvents.type}='unsubscribe'`),db.select().from(messages).orderBy(desc(messages.queuedAt)).limit(50)]);total=t[0]?.value??0;delivered=d[0]?.value??0;bounced=b[0]?.value??0;deferred=df[0]?.value??0;accepted=a[0]?.value??0;opens=o[0]?.value??0;clicks=c[0]?.value??0;complaints=cp[0]?.value??0;unsubs=u[0]?.value??0;recent=r}catch{dbError=true}const usable=databaseConfigured&&!dbError;const cards=[{l:"Messages",v:total,icon:BarChart3},{l:"Delivered",v:delivered,icon:MailCheck},{l:"Bounced",v:bounced,icon:ShieldAlert},{l:"Clicks",v:clicks,icon:MousePointerClick}];return <AppShell session={session}><div className="mb-7"><p className="mb-2 text-xs font-extrabold uppercase tracking-[.18em] text-zinc-400">Analytics</p><h1 className="text-3xl font-black tracking-[-.035em] sm:text-4xl">Reports</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-zinc-500">Only persisted message/event records are counted. Local MTA acceptance is never shown as delivery.</p></div>{!usable?<div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 px-5 py-4 text-sm text-amber-900 dark:border-amber-900/50 dark:bg-amber-950/25 dark:text-amber-200"><b>Database not connected.</b> Analytics stay empty rather than fabricated.</div>:null}<section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({l,v,icon:Icon})=><article key={l} className="premium-panel p-5"><div className="flex justify-between"><p className="text-sm font-bold text-zinc-500">{l}</p><Icon className="h-5 w-5 text-zinc-400"/></div><p className="mt-3 text-3xl font-black">{v.toLocaleString()}</p></article>)}</section><section className="mt-5 grid gap-5 xl:grid-cols-2"><article className="premium-panel p-6"><p className="text-xs font-extrabold uppercase tracking-[.16em] text-zinc-400">Delivery state</p><div className="mt-5 space-y-3">{[["Delivered (remote event)",delivered],["MTA accepted (local only)",accepted],["Deferred",deferred],["Bounced",bounced]].map(([l,v])=><div key={String(l)} className="flex justify-between rounded-xl border border-zinc-100 px-4 py-3 dark:border-zinc-800"><span className="text-sm font-bold">{l}</span><span className="font-black">{Number(v).toLocaleString()}</span></div>)}</div></article><article className="premium-panel p-6"><p className="text-xs font-extrabold uppercase tracking-[.16em] text-zinc-400">Engagement & compliance</p><div className="mt-5 grid grid-cols-2 gap-3">{[["Opens",opens],["Clicks",clicks],["Complaints",complaints],["Unsubscribes",unsubs]].map(([l,v])=><div key={String(l)} className="rounded-2xl bg-zinc-50 p-5 dark:bg-zinc-900"><p className="text-xs font-bold text-zinc-400">{l}</p><p className="mt-2 text-2xl font-black">{Number(v).toLocaleString()}</p></div>)}</div><p className="mt-4 text-xs leading-5 text-zinc-400">Open tracking is inherently imperfect.</p></article></section><section className="premium-panel mt-5 overflow-hidden"><div className="border-b border-zinc-100 p-5 dark:border-zinc-900"><h2 className="font-black">Recipient activity</h2><p className="mt-1 text-xs text-zinc-500">Latest 50 recipient message records.</p></div>{recent.length?<div className="overflow-x-auto"><table className="w-full min-w-[820px] text-left text-sm"><thead className="bg-zinc-50 text-[11px] font-extrabold uppercase tracking-[.12em] text-zinc-400 dark:bg-zinc-900/70"><tr><th className="px-5 py-3.5">Recipient</th><th>Status</th><th>Campaign</th><th>Accepted</th><th>Delivered</th><th>Error</th></tr></thead><tbody>{recent.map(m=><tr key={m.id} className="border-t border-zinc-100 dark:border-zinc-900"><td className="px-5 py-4 font-bold">{m.recipientEmail}</td><td className="text-xs font-bold">{m.status.replaceAll("_"," ")}</td><td className="font-mono text-xs text-zinc-500">{m.campaignId.slice(0,8)}</td><td className="text-xs text-zinc-500">{m.acceptedAt?new Intl.DateTimeFormat("en",{dateStyle:"short",timeStyle:"short"}).format(m.acceptedAt):"—"}</td><td className="text-xs text-zinc-500">{m.deliveredAt?new Intl.DateTimeFormat("en",{dateStyle:"short",timeStyle:"short"}).format(m.deliveredAt):"—"}</td><td className="max-w-[220px] truncate text-xs text-rose-500">{m.lastError||"—"}</td></tr>)}</tbody></table></div>:<div className="p-10 text-center text-sm text-zinc-500">No recipient activity yet.</div>}</section></AppShell>}
