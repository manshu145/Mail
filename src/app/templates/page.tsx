import Link from "next/link";import { FileText } from "lucide-react";import { redirect } from "next/navigation";import { desc } from "drizzle-orm";import { AppShell } from "@/components/app-shell";import { ResourceCreate } from "@/components/resource-create";import { db,databaseConfigured } from "@/db";import { templates } from "@/db/schema";import { getSession } from "@/lib/auth";export default async function TemplatesPage(){const session=await getSession();if(!session)redirect("/login");let rows:typeof templates.$inferSelect[]=[];let bad=false;if(databaseConfigured)try{rows=await db.select().from(templates).orderBy(desc(templates.updatedAt))}catch{bad=true}const usable=databaseConfigured&&!bad;return <AppShell session={session}><div className="mb-7 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-xs font-extrabold uppercase tracking-[.18em] text-zinc-400">Messaging</p><h1 className="text-3xl font-black tracking-[-.035em] sm:text-4xl">Templates</h1><p className="mt-2 text-sm text-zinc-500">Reusable HTML and plain-text content with personalization variables.</p></div><ResourceCreate disabled={!usable} endpoint="/api/resources/templates" title="Create template" buttonLabel="New template" fields={[{name:"name",label:"Template name",required:true},{name:"subject",label:"Default subject"},{name:"htmlBody",label:"HTML body",type:"textarea"},{name:"textBody",label:"Plain-text body",type:"textarea"}]}/></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.length?rows.map(r=><Link href={`/templates/${r.id}`} className="premium-panel block p-5 transition hover:-translate-y-0.5" key={r.id}><div className="grid h-10 w-10 place-items-center rounded-xl bg-amber-50 text-amber-700 dark:bg-amber-950/30 dark:text-amber-300"><FileText className="h-5 w-5"/></div><h2 className="mt-5 text-lg font-black">{r.name}</h2><p className="mt-1 line-clamp-2 text-sm text-zinc-500">{r.subject||"No default subject"}</p><div className="mt-5 text-[11px] font-extrabold uppercase tracking-[.1em] text-zinc-400">{r.htmlBody?"HTML":"No HTML"} · {r.textBody?"Text":"No text"}</div></Link>):<div className="premium-panel col-span-full grid min-h-72 place-items-center text-center"><div><FileText className="mx-auto h-8 w-8 text-zinc-400"/><h2 className="mt-4 font-black">No templates yet</h2></div></div>}</section></AppShell>}
